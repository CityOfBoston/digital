query FetchDeathCertificates($ids: [String!]!) {
  deathCertificates {
    certificates(ids: $ids) {
      id
      firstName
      lastName
      birthYear
      deathDate
      causeOfDeath
      age
    }
  }
}
