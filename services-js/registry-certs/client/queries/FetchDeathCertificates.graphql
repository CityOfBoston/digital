query FetchDeathCertificates($ids: [String!]!) {
  deathCertificates {
    certificates(ids: $ids) {
      id
      firstName
      lastName
      deathYear
      deathDate
      pending
      age
    }
  }
}
