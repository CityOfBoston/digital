query SearchDeathCertificates($query: String!, $page: Int!) {
  deathCertificates {
    search(query: $query, page: $page) {
      page
      pageSize
      pageCount
      resultCount
      results {
        id
        firstName
        lastName
        birthYear
        deathDate
        pending
        age
      }
    }
  }
}
