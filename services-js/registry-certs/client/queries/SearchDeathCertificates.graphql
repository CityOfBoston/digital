query SearchDeathCertificates($query: String!) {
  deathCertificates {
    search(query: $query) {
      id
      firstName
      lastName
      birthYear
      deathYear
      causeOfDeath
    }
  }
}
