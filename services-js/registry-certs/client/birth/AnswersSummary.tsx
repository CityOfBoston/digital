import React from 'react';
// import moment from 'moment';

// import { RequestInformation } from './QuestionsFlow';

// interface Props extends RequestInformation {}

// todo
export default function AnswersSummary(): JSX.Element {
  return (
    <div>
      [[ summary of provided information; select number of copies to order;
      order breakdown and pricing ]] [[ back button ]] [[ start over button ]]
      [[ enter checkout workflow ]]
    </div>
  );
}
