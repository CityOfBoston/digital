/**
 * Service to connect, process, etc ID Verification requests.
 */

export default class IdVerification {
  // employeeId: string;
  // constructor(
  //   employeeId: string | undefined,
  // ) {
  //   if (!employeeId) {
  //     throw new Error('ID Verificaiton Employee ID was not provided');
  //   }
  //   (this.employeeId = employeeId ? employeeId : '');
  // }
}
